components:
  parameters:
    nino:
      in: query
      required: true
      name: nino
      description: |
        National Insurance number, in the format AA999999A.
      schema:
        type: string
        example: "TC663795B"