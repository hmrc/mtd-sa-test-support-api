get:
  $ref: "./checkpoints_list.yaml"
  summary: List checkpoints
  security:
    - User-Restricted:
        - read:self-assessment
  tags:
    - Checkpoint for Vendor Data

post:
  $ref: "./checkpoints_create.yaml"
  summary: Create a checkpoint
  security:
    - User-Restricted:
        - write:self-assessment
  tags:
    - Checkpoint for Vendor Data